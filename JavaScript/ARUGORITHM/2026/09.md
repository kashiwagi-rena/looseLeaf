[B01 - A+B Problem](https://atcoder.jp/contests/tessoku-book/tasks/tessoku_book_bz)
```
function Main(input) {
  const [A, B] = input.trim().split(" ");
  console.log(Number(A) + Number(B));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - Multiplication 1](https://atcoder.jp/contests/abc169/tasks/abc169_a)
```
function Main(input) {
  const [A, B] = input.trim().split(" ");
  console.log(Number(A) * Number(B));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[002 - Sum of 3 Integers](https://atcoder.jp/contests/math-and-algorithm/tasks/math_and_algorithm_b)
```
function Main(input) {
  const [A, B, C] = input.trim().split(" ");
  console.log(Number(A) + Number(B) + Number(C));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - box](https://atcoder.jp/contests/abc180/tasks/abc180_a)
```
function Main(input) {
  const [A, B, C] = input.trim().split(" ");
  console.log(Number(A) - Number(B) + Number(C));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - ペナルティ](https://atcoder.jp/contests/past15-open/tasks/past202306_a)
```
function Main(input) {
  const [A, B] = input.trim().split(" ");
  console.log(5 * Number(A) + Number(B));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - Calc](https://atcoder.jp/contests/abc172/tasks/abc172_a)
```
function Main(input) {
  const A = input.trim().split("\n");
  const NumA = Number(A);
  console.log(NumA + (NumA * NumA) + (NumA * NumA * NumA));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - Happy New Year 2025](https://atcoder.jp/contests/abc387/tasks/abc387_a)
```
function Main(input) {
  const [A, B] = input.trim().split(" ");
  console.log((Number(A) + Number(B)) * (Number(A) + Number(B)));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - Determinant](https://atcoder.jp/contests/abc184/tasks/abc184_a)
```
function Main(input) {
  const [A, B] = input.trim().split("\n");
  const [a, b] = A.trim().split(" ");
  const [c, d] = B.trim().split(" ");
  console.log((Number(a) * Number(d)) - (Number(b) * Number(c)));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```