[	A - Nine or Nein](https://atcoder.jp/contests/abc471/tasks/abc471_a)
```
function Main(input) {
  const [a, b] = input.trim().split(" ");
  const A = Number(a);
  const B = Number(b);

  if ((A + B) === 9) {
    console.log("Nine");
  } else if ((A - B) === 9) {
    console.log("Nine");
  } else if ((A * B) === 9) {
    console.log("Nine");
  } else if ((A / B) === 9) {
    console.log("Nine");
  } else {
    console.log("Nein");
  }
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[B - Survey Tabulation](https://atcoder.jp/contests/abc471/tasks/abc471_b)
```
function Main(input) {
  const arr = input.trim().split("\n");
  const a = arr.shift();
  const b = arr;
  const unique = [...new Set(b.map(s => s.toLowerCase()))]
  let array = [];
  for (let i = 0; i <= unique.length - 1; i++) {
    let count = 0;
    for (let g = 0; g <= b.length - 1; g++) {
        const crr = b[g]
        if (unique[i] === crr.toLowerCase()) {
            count = count + 1;
        }
    }
    array[i] = [unique[i], count]
  }
  array.sort((a, b) => b[1] - a[1])
  console.log(array[0][1]);
}
```

[001 - Print 5+N](https://atcoder.jp/contests/math-and-algorithm/tasks/math_and_algorithm_a)
```
function Main(input) {
  const str = input.trim().split("\n");
  console.log(5 + Number(str));

}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[The First Problem](https://atcoder.jp/contests/tessoku-book/tasks/tessoku_book_a)
```
function Main(input) {
  const str = input.trim().split("\n");
  console.log(Number(str) * Number(str));

}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

[A - 立方体 (Cube)](https://atcoder.jp/contests/joi2022yo1b/tasks/joi2022_yo1b_a)
```
function Main(input) {
  const str = input.trim().split("\n");
  console.log(Number(str) * Number(str) * Number(str));
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
```

